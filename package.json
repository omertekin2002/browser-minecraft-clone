{
  "name": "voxelcraft",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A playable Minecraft-style voxel sandbox for the browser with a physically based WebGL2 shader pipeline.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
